{
  "name": "expense-splitter-mobile",
  "version": "1.0.0",
  "description": "Mobile wrapper for ExpenseSplitter app",
  "scripts": {
    "build": "cd expense-splitter && npm run build",
    "ios": "npm run build && npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/cli": "^5.5.0",
    "@capacitor/core": "^5.5.0",
    "@capacitor/ios": "^5.5.0",
    "@capacitor/storage": "^5.0.0",
    "@capacitor/share": "^5.0.0",
    "@capacitor/splash-screen": "^5.0.0",
    "@capacitor/status-bar": "^5.0.0"
  }
} 